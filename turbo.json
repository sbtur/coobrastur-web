{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "globalEnv": ["API_URL_COOB"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "env": ["API_URL_COOB", "CLIENT_ACCESS_KEY", "CLIENT_ACCESS_PASSWORD"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "env": ["API_URL_COOB", "CLIENT_ACCESS_KEY", "CLIENT_ACCESS_PASSWORD"]
    },
    "storybook": {
      "cache": false,
      "persistent": true
    }
  }
}
